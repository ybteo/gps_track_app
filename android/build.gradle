allprojects {
    repositories {
        google()
        mavenCentral()
        // maven { url 'https://maven.transistorsoft.com' } 
    }
    // dependencies {
    //     classpath 'com.android.tools.build:gradle:8.1.1'  // Use the latest Gradle plugin
    //     classpath 'com.google.gms:google-services:4.3.15'  // Add this for Firebase services
    // }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}



